require_relative "../lib/board"

describe Board do

  # describe ".new" do
  #   it "creates an array" do
  #     expect(Board.new.board).to be_a(Array)
  #   end
  #   it "creates 6 rows" do
  #     expect(Board.new.board.size).to eq(6)
  #   end
  #   it "creates 7 columns" do
  #     expect(Board.new.board[0].size).to eq(7)
  #   end
  #   it "creates a board with nul values in each space" do
  #     expect(Board.new.board[0][0]).to eq("")
  #     expect(Board.new.board[5][6]).to eq("")
  #   end
  # end

end
